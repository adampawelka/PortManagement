# ============================================================================
# PORT MANAGEMENT SYSTEM - ENVIRONMENT VARIABLES
# ============================================================================

# DATABASE CONFIGURATION
# ----------------------------------------------------------------------------

DB_HOST=postgres
DB_PORT=5432
DB_NAME=portmanagementdb
DB_USER=portapp
DB_PASSWORD=PortManagement123!

MONGO_HOST=10.9.20.216
MONGO_PORT=27017
MONGO_DB=portoemdb
MONGO_USER=oem_admin
MONGO_PASSWORD=PORTMANAGEMENT3DLE04
MONGODB_URI=mongodb://oem_admin:PORTMANAGEMENT3DLE04@10.9.20.216:27017/portoemdb?authSource=admin

# API CONFIGURATION
# ----------------------------------------------------------------------------
PORT=5161
NODE_ENV=production

# EXTERNAL SERVICES
BACKEND_API_URL=http://backend:5000
SCHEDULING_API_URL=http://scheduling:5107

# SECURITY
JWT_SECRET=LVAR2f6MoYTtL+GiitLI5Eos5y4ws7N3zXT6/1HwNvs=
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5000,http://scheduling:5107,http://localhost:5161

# AUTH0 CONFIGURATION (if using)
AUTH0_DOMAIN=3dl-e-04.eu.auth0.com
AUTH0_AUDIENCE=http://3dl-e-04api
AUTH0_CLIENT_ID=your_client_id_here

# LOGGING
LOG_LEVEL=info
LOG_FILE=/opt/port-management/logs/oem.log

# ============================================================================
# ABOUT JWT_SECRET AND CORS_ORIGINS:
# 
# JWT_SECRET: Used for signing JSON Web Tokens. Generate a secure random string:
#   On Linux: openssl rand -base64 32
#   Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# CORS_ORIGINS: Comma-separated list of allowed origins. Include:
#   - Your frontend URL (e.g., http://localhost:5173)
#   - Your backend URL if making direct API calls
#   - Your production domain when deployed
#
# ============================================================================